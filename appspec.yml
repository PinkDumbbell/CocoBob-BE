version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/deploy
    overwrite: yes

hooks:
  BeforeInstall:
    - location: ~/CocoBob-BE-CI,CD-test/script/deploy.sh
      timeout: 300
      runas: root
version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/deploy
    overwrite: yes

hooks:
  BeforeInstall:
    - location: /deployment-archive/deploy.sh
      timeout: 300
      runas: root